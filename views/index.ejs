<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tobi backend</title>

    <style>
      * {
        margin: 0;
        padding: 0;
      }

      body {
        font-family: "Inter", sans-serif;
        background-color: #f0f4f8;
        color: #333;
      }

      h1 {
        color: #2c5282;
        margin-bottom: 1.5rem;
        text-align: center;
      }

      p {
        text-align: center;
      }

      span {
        border: 1px solid #e2e8f0; 
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        padding: 4px;
        background-color: #e6fffa;
        font-weight: 700;
        letter-spacing: 2px;
      }
    </style>
  </head>

  <body>
    <h1>Welcome Esemokia</h1>

    <p>Your received code is <span><%= code %></span></p>
  </body>
</html>
